// src/components/chip/config.js

import { PREFIX } from '../../core/config'

/**
 * Default configuration for the Chip component
 */
const defaultConfig = {
  componentName: 'chip',
  prefix: PREFIX
}

export default defaultConfig
