#!/usr/bin/env bun
/**
 * Compatibility entry point for the bundle analyzer.
 * Build first, then run this script (or `bun run size:check`).
 * Uses packed consumer imports and writes analysis/package-size.json.
 */
import "./check-package-size";
